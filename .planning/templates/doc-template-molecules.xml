<doc-molecules>
  <molecule id="FilesBlock">
    <component>Uses FileAtom for each entry inside a files list.</component>
  </molecule>
  <molecule id="CommandsBlock">
    <component>Aggregates CommandAtom nodes.</component>
  </molecule>
  <molecule id="DecisionsBlock">
    <component>List of DecisionAtom entries.</component>
  </molecule>
  <molecule id="TasksBlock">
    <component>Wraps TaskAtom entries inside a table or list.</component>
  </molecule>
  <molecule id="KeywordsBlock">
    <component>Inline KeywordAtom nodes for quick search tags.</component>
  </molecule>
  <molecule id="VerificationBlock">
    <component>Contains VerificationAtom entries.</component>
  </molecule>
  <molecule id="ReferencesBlock">
    <component>Aggregates ReferenceAtom entries</component>
  </molecule>
</doc-molecules>
